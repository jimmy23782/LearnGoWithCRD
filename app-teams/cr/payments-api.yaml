apiVersion: platform.mac.com/v1
kind: Tenant
metadata:
  name: payments-api
  namespace: default
spec:
  teamName: payments
  environment: dev
  apiName: payments-api
  backend:
    url: https://payments.example.com
  authentication:
    type: jwt